Algorithme Palandrom
Var 
mot = string;
motInverse= string;

Begin

for (i = mot.length - 1, i >= 0, i--){
   motInverse := motInversze + mot[i];
    }

for (i = 0, i<mot.length, i++){
    if(mot[i] != motInverse[i]);
      return false;
}
return true;

End